/**
 * @file Types for the recipe grid client component.
 */

import type { RecipeListItem } from "@/types/recipes";

/**
 * Props for the recipes grid client component.
 */
export type RecipeGridClientProps = {
  initialRecipes: RecipeListItem[];
};
